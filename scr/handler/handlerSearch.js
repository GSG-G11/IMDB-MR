const fs = require('fs');
const path = require('path');
const querystring = require('querystring');

const searchHandler = (response, request) => {
//   let allTheData = '';
//   request.on('data', (chunkOfData) => {
//     allTheData += chunkOfData;
//   });
};

module.exports = searchHandler;
